'use strict';

const webElementsReact = require('..');
const assert = require('assert').strict;

assert.strictEqual(webElementsReact(), 'Hello from webElementsReact');
console.info('webElementsReact tests passed');
